{
    "sphinx-needs.needsJson": "${workspaceFolder}/needs.json",
    "sphinx-needs.srcDir": "${workspaceFolder}",
    "sphinx-needs.activateFiles": [
        "restructuredtext",
        "python",
        "plaintext"
    ]
}