{
    "sphinx-needs.needsJson": "${workspaceFolder}/needs_small.json",
    "sphinx-needs.srcDir": "${workspaceFolder}",
    "sphinx-needs.activateFiles": [
        "restructuredtext",
        "python",
        "plaintext"
    ],
    "sphinx-needs.explorerOptions": ["type", "description"]
}